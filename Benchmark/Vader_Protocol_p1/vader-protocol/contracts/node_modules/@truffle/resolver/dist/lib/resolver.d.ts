import { ResolverSource } from "./source";
declare type Callback = (err: Error | null, body?: string, filePath?: string, source?: ResolverSource) => void;
export declare class Resolver {
    options: any;
    sources: ResolverSource[];
    constructor(options: any);
    require(import_path: string, search_path: string): never;
    resolve(importPath: string, importedFrom: string, callback: Callback): void;
    private _resolve;
}
export {};
