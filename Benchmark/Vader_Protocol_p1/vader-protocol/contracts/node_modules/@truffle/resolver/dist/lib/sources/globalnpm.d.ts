import { ResolverSource } from "../source";
export declare class GlobalNPM implements ResolverSource {
    require(importPath: string): any;
    resolve(importPath: string): Promise<{
        body: string;
        filePath: string;
    }>;
    resolve_dependency_path(importPath: string, dependencyPath: string): string;
}
