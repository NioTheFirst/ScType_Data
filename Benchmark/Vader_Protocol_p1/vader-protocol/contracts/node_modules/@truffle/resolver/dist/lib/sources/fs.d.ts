import { ResolverSource } from "../source";
export declare class FS implements ResolverSource {
    workingDirectory: string;
    contractsBuildDirectory: string;
    constructor(workingDirectory: string, contractsBuildDirectory: string);
    require(importPath: string, searchPath?: string): any;
    getContractName(sourcePath: string, searchPath?: string): any;
    resolve(importPath: string, importedFrom: string): Promise<{
        body: undefined;
        filePath: undefined;
    }>;
    resolve_dependency_path(importPath: string, dependencyPath: string): string;
}
